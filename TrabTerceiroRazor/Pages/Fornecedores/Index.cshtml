@page
@model TrabTerceiroRazor.Pages.Fornecedores.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fornecedor[0].ForNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Fornecedor) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ForNome)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ForId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ForId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ForId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
